class Task {
    constructor(description, title, priority, dueDate, done) {
        this.description = description;
        this.title = title;
        this.priority = priority;
        this.dueDate = dueDate;
        this.done = done;
    }
    // Add a funcionality
}
export {Task};